<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @zone.name %>
</p>
<table class="table">
  <thead>
    <tr>
      <th>Escuelas</th>
    </tr>
  </thead>
  <tbody>
    <% @zone.schools.each do |school| %>
      <tr>
        <td><%= school.name %></td>        
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_zone_path(@zone) %> |
<%= link_to 'Back', zones_path %>
